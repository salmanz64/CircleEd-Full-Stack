# CircleEd Architecture & Integration Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                                │
│                    http://localhost:3000                            │
│                                                                      │
│   ┌────────────────────────────────────────────────────────────┐   │
│   │            NEXT.JS FRONTEND APPLICATION                   │   │
│   │                                                            │   │
│   │  ┌──────────────────────────────────────────────────────┐ │   │
│   │  │           React Components                         │ │   │
│   │  │  ├─ Marketplace Page                               │ │   │
│   │  │  ├─ Dashboard Page                                 │ │   │
│   │  │  ├─ Chat Page                                      │ │   │
│   │  │  ├─ Bookings Page                                  │ │   │
│   │  │  └─ Profile Page                                   │ │   │
│   │  └──────────────────────────────────────────────────────┘ │   │
│   │                         │                                  │   │
│   │                         ▼                                  │   │
│   │  ┌──────────────────────────────────────────────────────┐ │   │
│   │  │      API Service Layer (lib/api.ts)                │ │   │
│   │  │                                                      │ │   │
│   │  │  skillsAPI • usersAPI • sessionsAPI                │ │   │
│   │  │  chatsAPI  • transactionsAPI • authAPI             │ │   │
│   │  └──────────────────────────────────────────────────────┘ │   │
│   └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬─────────────────────────────────────┘
                                 │
                    HTTP/CORS (Port 8000)
                                 │
┌────────────────────────────────▼─────────────────────────────────────┐
│                      FASTAPI BACKEND SERVER                         │
│                    http://localhost:8000                            │
│                                                                      │
│   ┌────────────────────────────────────────────────────────────┐   │
│   │                   API Router (/api/v1)                    │   │
│   │                                                            │   │
│   │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │   │
│   │  │ Skills       │  │ Users        │  │ Sessions     │   │   │
│   │  │ Endpoints    │  │ Endpoints    │  │ Endpoints    │   │   │
│   │  └──────────────┘  └──────────────┘  └──────────────┘   │   │
│   │                                                            │   │
│   │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │   │
│   │  │ Chats        │  │ Transactions │  │ Auth         │   │   │
│   │  │ Endpoints    │  │ Endpoints    │  │ Endpoints    │   │   │
│   │  └──────────────┘  └──────────────┘  └──────────────┘   │   │
│   └────────────────────────────────────────────────────────────┘   │
│                         │                                           │
│                         ▼                                           │
│   ┌────────────────────────────────────────────────────────────┐   │
│   │                SQLAlchemy ORM                             │   │
│   │                                                            │   │
│   │  ├─ User Model         ├─ Chat Model                     │   │
│   │  ├─ Skill Model        ├─ Message Model                  │   │
│   │  ├─ SkillReview Model  ├─ Transaction Model              │   │
│   │  └─ Session Model                                         │   │
│   └────────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬─────────────────────────────────────┘
                                 │
                      PostgreSQL Protocol
                                 │
┌────────────────────────────────▼─────────────────────────────────────┐
│                    PostgreSQL Database                              │
│                     Database: circleed                              │
│                  Port: 5432 (localhost)                             │
│                                                                      │
│   ┌────────────────────────────────────────────────────────────┐   │
│   │                    Database Tables                         │   │
│   │                                                            │   │
│   │  users        ├─ id (PK)                                 │   │
│   │              ├─ email                                     │   │
│   │              ├─ full_name                                 │   │
│   │              ├─ tokens_balance                            │   │
│   │              └─ avatar_url                                │   │
│   │                                                            │   │
│   │  skills       ├─ id (PK)                                 │   │
│   │              ├─ title                                     │   │
│   │              ├─ teacher_id (FK)                           │   │
│   │              ├─ category                                  │   │
│   │              └─ tokens_per_session                        │   │
│   │                                                            │   │
│   │  sessions     ├─ id (PK)                                 │   │
│   │              ├─ skill_id (FK)                             │   │
│   │              ├─ student_id (FK)                           │   │
│   │              ├─ teacher_id (FK)                           │   │
│   │              └─ scheduled_at                              │   │
│   │                                                            │   │
│   │  chats        ├─ id (PK)                                 │   │
│   │              ├─ user_1_id (FK)                            │   │
│   │              └─ user_2_id (FK)                            │   │
│   │                                                            │   │
│   │  + More tables (reviews, transactions, messages)         │   │
│   └────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Component Data Flow

### Marketplace Page Example

```
┌─────────────────────────────────────────────────────────────┐
│           Marketplace Page (marketplace/page.tsx)           │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
        ┌──────────────────────────────────┐
        │      Component State             │
        │  ├─ skills: Skill[]             │
        │  ├─ loading: boolean             │
        │  ├─ error: string | null         │
        │  ├─ filters: {...}               │
        │  └─ searchQuery: string          │
        └──────────────────────────────────┘
                         │
                         ▼
        ┌──────────────────────────────────┐
        │      useEffect Hook              │
        │  Triggers on filter changes      │
        └──────────────────────────────────┘
                         │
                         ▼
        ┌──────────────────────────────────┐
        │    skillsAPI.getAll({            │
        │      category,                   │
        │      level,                      │
        │      language,                   │
        │      search                      │
        │    })                            │
        └──────────────────────────────────┘
                         │
                         ▼ (HTTP/Fetch)
        ┌──────────────────────────────────┐
        │  Backend Route                   │
        │  GET /api/v1/skills              │
        │  with query parameters           │
        └──────────────────────────────────┘
                         │
                         ▼
        ┌──────────────────────────────────┐
        │  Database Query                  │
        │  SELECT * FROM skills            │
        │  WHERE filters applied           │
        └──────────────────────────────────┘
                         │
                         ▼
        ┌──────────────────────────────────┐
        │  SQL Result                      │
        │  List of Skill objects           │
        │  with teacher relationships      │
        └──────────────────────────────────┘
                         │
                         ▼ (JSON Response)
        ┌──────────────────────────────────┐
        │  setSkills(data)                 │
        │  setLoading(false)               │
        └──────────────────────────────────┘
                         │
                         ▼
        ┌──────────────────────────────────┐
        │  Render SkillCard Components     │
        │  for each skill in state         │
        └──────────────────────────────────┘
                         │
                         ▼
        ┌──────────────────────────────────┐
        │  Display Skills to User          │
        │  with teacher info, ratings      │
        └──────────────────────────────────┘
```

---

## User Authentication Flow (Future Implementation)

```
┌─────────────────────────────────────────────────────────┐
│           Login/Register Page                          │
│           Submit credentials form                      │
└─────────────────────────────────────────────────────────┘
                      │
                      ▼
          ┌──────────────────────────────┐
          │  authAPI.login() or          │
          │  authAPI.register()          │
          └──────────────────────────────┘
                      │
                      ▼ (POST request)
          ┌──────────────────────────────┐
          │  Backend Auth Endpoint       │
          │  /api/v1/auth/login          │
          │  /api/v1/auth/register       │
          └──────────────────────────────┘
                      │
                      ▼
          ┌──────────────────────────────┐
          │  Validate Credentials        │
          │  Hash Password               │
          │  Generate JWT Token          │
          └──────────────────────────────┘
                      │
                      ▼
          ┌──────────────────────────────┐
          │  Return:                     │
          │  {                           │
          │    access_token: "JWT...",   │
          │    token_type: "bearer",     │
          │    user: { ... }             │
          │  }                           │
          └──────────────────────────────┘
                      │
                      ▼
          ┌──────────────────────────────┐
          │  Store token in:             │
          │  - localStorage              │
          │  - Context/Store             │
          │  - Request headers           │
          └──────────────────────────────┘
                      │
                      ▼
          ┌──────────────────────────────┐
          │  All future API requests     │
          │  include JWT in headers:     │
          │  Authorization: Bearer JWT   │
          └──────────────────────────────┘
```

---

## Request/Response Flow Diagram

```
Frontend Request
     │
     ├─ URL: http://localhost:8000/api/v1/skills
     ├─ Method: GET
     ├─ Query: ?category=Programming&level=Beginner
     └─ Headers: 
         ├─ Content-Type: application/json
         ├─ Accept: application/json
         └─ Origin: http://localhost:3000  (CORS)
     │
     ▼
Backend Processing
     │
     ├─ CORS Middleware: Check origin ✓
     ├─ Router: Match GET /skills ✓
     ├─ Handler: get_skills(category, level, ...)
     ├─ Database: Query skills with filters
     ├─ Transform: Convert to Pydantic schemas
     └─ Response: JSON list of skills
     │
     ▼
Frontend Response
     │
     ├─ Status: 200 OK
     ├─ Headers:
     │  ├─ Content-Type: application/json
     │  └─ Access-Control-Allow-Origin: http://localhost:3000
     │
     ├─ Body: [
     │    {
     │      "id": 1,
     │      "title": "JavaScript",
     │      "teacher": { "id": 5, "full_name": "John" },
     │      ...
     │    },
     │    ...
     │  ]
     │
     ▼
JavaScript Promise
     │
     ├─ parse JSON
     ├─ type-check (TypeScript)
     ├─ setState(skills)
     ├─ Component re-renders
     └─ DOM updates with new skills
     │
     ▼
User sees updated skills
```

---

## File Organization

```
CircleEd-Full-Stack/
│
├── 📁 backend/
│   ├── 📁 app/
│   │   ├── 📁 api/
│   │   │   └── 📁 v1/
│   │   │       ├── api.py              ← Routes registration
│   │   │       └── 📁 endpoints/
│   │   │           ├── skills.py       ← Skill endpoints
│   │   │           ├── users.py        ← User endpoints
│   │   │           ├── sessions.py     ← Session endpoints
│   │   │           ├── chats.py        ← Chat endpoints
│   │   │           ├── transactions.py ← Transaction endpoints
│   │   │           └── auth.py         ← Auth endpoints
│   │   ├── 📁 models/
│   │   │   ├── user.py
│   │   │   ├── skill.py
│   │   │   ├── session.py
│   │   │   ├── chat.py
│   │   │   └── ...
│   │   ├── 📁 schemas/
│   │   │   ├── user.py
│   │   │   ├── skill.py
│   │   │   └── ...
│   │   ├── 📁 core/
│   │   │   ├── config.py     ← Settings
│   │   │   ├── database.py   ← DB Connection
│   │   │   └── security.py   ← JWT
│   │   └── main.py
│   ├── run.py
│   ├── requirements.txt
│   └── .env                  ← ✅ CREATED
│
├── 📁 frontend/
│   ├── 📁 app/
│   │   ├── 📁 (dashboard)/
│   │   │   ├── 📁 marketplace/
│   │   │   │   └── page.tsx  ← ✅ UPDATED (real API)
│   │   │   ├── 📁 dashboard/
│   │   │   │   └── page.tsx  ← ✅ UPDATED (real API)
│   │   │   └── ...
│   │   └── ...
│   ├── 📁 components/
│   │   ├── SkillCard.tsx
│   │   └── ui/
│   │       └── button.tsx    ← ✅ FIXED
│   ├── 📁 lib/
│   │   ├── api.ts            ← ✅ CREATED
│   │   └── utils.ts
│   ├── package.json
│   ├── next.config.js
│   └── .env.local            ← ✅ CREATED
│
├── start-dev.bat             ← ✅ CREATED (Windows)
├── start-dev.sh              ← ✅ CREATED (Mac/Linux)
│
└── 📁 Documentation/
    ├── INTEGRATION_SETUP.md   ← ✅ CREATED
    ├── INTEGRATION_COMPLETE.md ← ✅ CREATED
    ├── GETTING_STARTED.md     ← ✅ CREATED
    └── CHANGES_SUMMARY.md     ← ✅ CREATED
```

---

## Technology Stack

```
Frontend
├── Next.js 15         (React Framework)
├── React 18.3         (UI Library)
├── TypeScript 5.3     (Type Safety)
├── Tailwind CSS       (Styling)
├── Zustand            (State Management)
├── React Hook Form    (Form Handling)
└── Fetch API          (HTTP Requests)

Backend
├── FastAPI            (Web Framework)
├── Python 3.8+        (Language)
├── SQLAlchemy         (ORM)
├── Pydantic           (Data Validation)
├── PostgreSQL Driver  (Database)
└── CORS Middleware    (Cross-origin support)

Database
├── PostgreSQL 12+     (Relational Database)
└── Database: circleed

Infrastructure
├── Localhost:3000     (Frontend)
├── Localhost:8000     (Backend)
└── Localhost:5432     (Database)
```

---

## Status Summary

| Component | Status | Details |
|-----------|--------|---------|
| Database Integration | ✅ Complete | PostgreSQL circleed configured |
| API Service | ✅ Complete | Full typed API layer |
| Skills Display | ✅ Complete | Real data from backend |
| User Dashboard | ✅ Complete | Real user data |
| Error Handling | ✅ Complete | Try-catch with UI feedback |
| Loading States | ✅ Complete | Spinners and skeletons |
| CORS | ✅ Complete | Frontend-Backend communication |
| Environment Config | ✅ Complete | .env files for both |
| Documentation | ✅ Complete | 4 markdown guides |
| Quick Start Scripts | ✅ Complete | Windows & Mac/Linux |

---

**Your full-stack application is ready to use! 🎉**
